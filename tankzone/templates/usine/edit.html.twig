{% extends 'base.html.twig' %}

{% block title %}Edit Usine{% endblock %}

{% block main %}
    <h1>Edit Usine</h1>

    {{ include('usine/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_usine_index') }}">back to list</a>
    {% if is_granted('ROLE_ADMIN') %}
        {{ include('usine/_delete_form.html.twig') }}
    {% endif %}

{% endblock %}
